# exporter

A simple data export CLI.

## Usage

```bash
python cli.py --out results.json
python cli.py --out results.csv --format csv
```

## Flags

- `--out` - path to the output file (required)
- `--format` - output format: `json` or `csv` (default: `json`)
