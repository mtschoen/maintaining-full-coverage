# widget-scoring

Utilities for scoring widgets.

## Usage

```python
from lib import total_score

total_score([1, 2, 3])  # -> 6
```

`total_score` takes a list of numeric values and returns their combined score.
