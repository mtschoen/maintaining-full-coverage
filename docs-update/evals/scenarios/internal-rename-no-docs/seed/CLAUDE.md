# Project instructions

A small scoring library.

## Public API

The only supported entry point is `total_score`, which returns the combined
score for a list of numeric values. Treat everything else as internal.

## Conventions

- Keep functions small and focused.
- Prefer full words over abbreviations in identifiers.
